/** Firebase error code received when a user tries to register but the email already exists */
export const AUTH_EMAIL_ALREADY_IN_USE = 'auth/email-already-in-use';

/** Firebase error code received when a user tries to login but the credentials are invalid */
export const AUTH_INVALID_LOGIN_CREDENTIALS = 'auth/invalid-login-credentials';

/** Firebase error code received when a user has tried to login unsuccessfully to a same account */
export const AUTH_TOO_MANY_ATTEMPTS = 'auth/too-many-requests';
