import { ArrayUppercasePipe } from './array-uppercase.pipe';

describe('ArrayUppercasePipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayUppercasePipe();
    expect(pipe).toBeTruthy();
  });
});
