import { ArrayTitlecasePipe } from './array-titlecase.pipe';

describe('ArrayTitlecasePipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayTitlecasePipe();
    expect(pipe).toBeTruthy();
  });
});
