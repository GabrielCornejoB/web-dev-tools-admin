import { ToolsPageComponent } from '@tools/pages/tools-page/tools-page.component';

describe('ToolsPageComponent', () => {
  let component: ToolsPageComponent;

  beforeEach(() => {
    component = new ToolsPageComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
